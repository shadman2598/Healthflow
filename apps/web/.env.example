# Leave NEXT_PUBLIC_API_URL unset — the web app calls /api/backend/* which
# Next.js rewrites to the Express API (same-origin cookies).

# Optional override for the rewrite destination (server-side only):
# API_PROXY_ORIGIN=http://127.0.0.1:4000
